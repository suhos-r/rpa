# Closing Step (Internal)

File: `Workflows/FinalizeRun.xaml`. Runs once at the very end.

- **Write final results** — Save the full result table one last time so nothing is lost.
- **Log campaign summary** — Write in the diary: run name, how many tries were done, and the picture-board address to open.
